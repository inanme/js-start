"use strict";

export const incr = x => x + 1;
export const decr = x => x - 1;
export default x => x;

// import identity, * as r from './export-examples';
//
// console.log(r.incr(2));
// console.log(identity(2));
// console.log(r.decr(2));
